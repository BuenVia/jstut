/* 

Destructuring Arrays is a way of of unpacking values from an array or object and storing them in separate variables

*/

// An array
const arr = [2, 3, 4]
// The above array destructured and asigned to individual variables
const [x, y, z] = arr
console.log(x)